apiVersion: v1
kind: Service
metadata:
  name: gitlab-service
  namespace: gitlab
  labels:
    name: gitlab-service
spec:
  type: LoadBalancer
  selector:
    name: gitlab-service
  ports:
    - name: http
      port: 90
      targetPort: http
    - name: ssh
      port: 1022
      targetPort: ssh
  externalIPs:
    - 192.168.222.132
